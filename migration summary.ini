Session Summary: Migration from SaveableParameters to saveloadlib
New API in saveloadlib
saveloadlib.h

Added ctx-based save_recursive(prefix, len, cb, ctx, scope) overload
Added sl_save_to_linkedlist(root, out, scope) inline helper
saveload_settings.h

Added C++14 compatibility path (#if __cplusplus >= 201703L) — C++17 uses if constexpr; C++14 uses tag-dispatch partial specializations (sl_type_tag<T>, sl_parser<T,Tag>, sl_formatter<T,Tag>)
Migrated: usb_teensy_clocker
behaviour_base.h

Added SHStorage<16,160> as base class
Replaced entire saveable_parameters / setup_saveable_parameters() block with setup_saveable_settings()
Moved #include "saveload_settings.h" before #include [Parameter.h](http://_vscodecontentref_/5)" (critical include order fix)
New save_pattern_add_lines() calls sl_save_to_linkedlist
New load_parse_key_value() tokenizes and calls load_line()
behaviour_base.cpp

Stubbed create_saveable_parameters_recall_selector() returning nullptr
behaviour_euclidianrhythms.h — setup_saveable_settings(): base + register_child(sequencer)

behaviour_progression.h — 5 LSaveableSetting registrations

behaviour_clocked.h — mixin registers: divisor, delay_ticks, auto_restart_on_change, pause_on

behaviour_midibass.h — mixin registers: drone, machinegun

behaviour_polyphonic.h — mixin registers: Output 1–4

behaviour_cvoutput.h — base + Polyphonic + Slew/SlewRate/GateOutput per channel

behaviour_kawair50.h — base + DividedClocked + drum map entries; removed stale #include <SaveableParameters.h>

behaviour_midimuso_4mv.h — base + DividedClocked + Polyphonic

behaviour_beatstep.h — base + DividedClocked + sysex parameters loop; removed stale #include "SaveableParameters.h"

behaviour_cvinput.h — base + 9 scale/chord settings under #ifdef ENABLE_SCALES

behaviour_neutron.h — base + MIDIBassBehaviour

behaviour_bamble.h — SaveableParameterPatternEnabled base changed to SaveableSettingBase; 8 settings + pattern enable loop

behaviour_skulptsynth.h — base + Clock Enabled bool

behaviour_manager.h — new setup_saveable_settings(), save_pattern_add_lines(), load_parse_line()

behaviour_manager.cpp — added setup_saveable_settings() call; recall UI loop and has_saveable_parameters() condition commented out

main.cpp — removed stale behaviour_manager->setup_saveable_parameters() call

storage.cpp — removed parameter_manager->save_pattern_parameter_inputs_add_lines() and commented out parameter_manager->load_parse_line()

Migrated: parameters library
CVOutputParameter.h

Replaced load_key_fragment() + save_pattern_add_lines() (both calling non-existent FloatParameter::load_key_fragment) with a setup_saveable_settings() override registering channel as LSaveableSetting<byte> when configurable=true