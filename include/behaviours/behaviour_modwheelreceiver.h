/*#include "behaviours/behaviour_base.h"

class ModWheelReceiver : virtual public DeviceBehaviourUltimateBase {

    virtual void receive_control_change (uint8_t inChannel, uint8_t inNumber, uint8_t inValue) {
        Serial.printf("receive_control_change(%i, %i, %i) in %s\n", inChannel, inNumber, inValue, this->get_label());
        this->send

    }
};
*/